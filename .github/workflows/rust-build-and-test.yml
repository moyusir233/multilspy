name: Rust Build and Test

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          override: true
          components: clippy, rust-analyzer

      - name: Clippy
        run: |
          cd rust && cargo clippy --all --all-features

      - name: Cargo test
        run: |
          cd rust && cargo test
